---
"@moonshot-ai/kimi-code": minor
---

Add configurable AI session title generation: set `[session_title].enabled` in config.toml, optionally pick a `model` to generate titles with, and run `/title generate` to regenerate one; titles are also generated for sessions that are not actively running.
