---
"@moonshot-ai/kimi-code": minor
---

Choose the model used for AI session titles with `[session_title].model`, turn generation off with `[session_title].enabled = false`, and regenerate one on demand with `/title generate`; titles are also generated for sessions that are not actively running.
