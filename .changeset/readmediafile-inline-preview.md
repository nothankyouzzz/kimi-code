---
"@moonshot-ai/kimi-code": patch
---

Show inline image previews for `ReadMediaFile` results in terminals that support the Kitty or iTerm2 inline image protocol. Replayed sessions resolve the persisted `blobref:` media references back from the session blob store, so previews work after a resume too.
