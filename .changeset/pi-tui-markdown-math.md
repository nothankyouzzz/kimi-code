---
"@moonshot-ai/pi-tui": minor
---

Add display-math ($$...$$) and inline-math ($...$) tokenizers to the Markdown component, with optional theme hooks to render them as image lines or replacement text; unclosed blocks and math nested in lists or quotes fall back to source-style rendering.
